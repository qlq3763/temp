1. use $? to get the last exit number.

2. A bug:
int isPrime(int number)
{
    int i;
    for(i = 2; i*i < number; i++) {

=>

int isPrime(int number)
{
    if (number < 2) return 0;    
    int i;
    for(i = 2; i*i <= number; i++) {

What if number is 1 or number is 4?

